# Notebooks

This directory contains date-indexed notebooks with various analyses and visualizations.
Most of these will contain outdated API and is only meant to keep record.
Ideally, some of the plotting scripts and data will be transferred (and runnable) from the `./scripts` directory.